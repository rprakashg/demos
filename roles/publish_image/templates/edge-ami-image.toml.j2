name            = "edge-ami-image"
description     = "Blueprint for Edge AMI image"
version         = "0.0.1"
distro          = "rhel-9.4"
modules = []
groups = []

[[packages] 
name = "cloud-init"

[[customizations.user]]
name = "{{ admin_user }}"
password = "{{ admin_password_e}}"
groups = ["wheel"]
key = "{{ admin_ssh_key }}"